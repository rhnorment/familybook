database:
  override:
    - bin/setup
    - bundle exec rake db:migrate RAILS_ENV=test
test:
  override:
    - COVERAGE=true bin/rake
deployment:
  staging:
    branch: master
    commands:
      - bin/deploy staging
